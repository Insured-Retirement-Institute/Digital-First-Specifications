# Modified by Cursor: shared MethodNotAllowedError response
components:
  responses:
    MethodNotAllowedError:
      description: "Method Not Allowed — the HTTP method is not supported on this resource."
      headers:
        correlationId:
          $ref: './correlationId_v1.yaml#/components/headers/correlationId'
      content:
        application/json:
          schema:
            $ref: './Error_v1.yaml#/components/schemas/Error'
