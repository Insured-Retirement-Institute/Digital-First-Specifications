# Modified by Cursor: shared ServiceUnavailableError response (v2)
components:
  responses:
    ServiceUnavailableError:
      description: "Service Unavailable — the service is temporarily unable to handle the request."
      headers:
        correlationId:
          $ref: './correlationId_v1.yaml#/components/headers/correlationId'
      content:
        application/json:
          schema:
            $ref: './Error_v2.yaml#/components/schemas/Error'
