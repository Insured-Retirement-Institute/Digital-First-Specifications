# Modified by Cursor: shared UnsupportedMediaTypeError response
components:
  responses:
    UnsupportedMediaTypeError:
      description: "Unsupported Media Type — the request Content-Type is not supported."
      headers:
        correlationId:
          $ref: './correlationId_v1.yaml#/components/headers/correlationId'
      content:
        application/json:
          schema:
            $ref: './Error_v1.yaml#/components/schemas/Error'
