# Modified by Cursor: shared BadRequestError response
components:
  responses:
    BadRequestError:
      description: "Bad Request — the request payload is malformed or fails schema validation."
      headers:
        correlationId:
          $ref: './correlationId_v1.yaml#/components/headers/correlationId'
      content:
        application/json:
          schema:
            $ref: './Error_v1.yaml#/components/schemas/Error'
